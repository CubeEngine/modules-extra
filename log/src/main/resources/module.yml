name: ${project.name}
description: ${project.description}
version: ${project.version}
source-version: ${githead.branch}-${githead.commit}
build-timestamp: "${timestamp}"
dependencies:
- bigdata
services:
- de.cubeisland.engine.core.module.service.Selector
